<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.kurbankuuser">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Depan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TambahKurban"
            android:label="Tambah Kurban">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Depan" />
        </activity>
        <activity
            android:name=".Pesanan"
            android:label="Pesanan Kurban">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Depan" />
        </activity>
        <activity
            android:name=".DaftarHewan"
            android:label="Daftar Hewan">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Depan" />
        </activity>
        <activity android:name=".Konfirmasi"></activity>
    </application>

</manifest>